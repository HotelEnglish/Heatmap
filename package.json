{
  "name": "china-heatmap",
  "version": "1.0.0",
  "description": "中国城市热力图可视化工具",
  "scripts": {
    "start": "serve .",
    "build": "echo 'No build required'",
    "vercel-build": "mkdir -p dist && cp -r * dist && cp -r .[^.]* dist"
  },
  "dependencies": {
    "serve": "^14.2.1"
  },
  "engines": {
    "node": ">=14"
  }
}
